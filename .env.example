# Local development reference only. For deployment, set variables in the Vercel/Netlify dashboard.
# Backend variables belong to the backend project.
# Frontend variables belong to the frontend project.

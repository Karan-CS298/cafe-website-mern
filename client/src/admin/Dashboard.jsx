import {useEffect,useState} from 'react'; import {api} from '../services/api'; export default function Dashboard(){const [d,setD]=useState({products:0,categories:0,best:0});useEffect(()=>{Promise.all([api.get('/admin/products'),api.get('/admin/categories')]).then(([p,c])=>setD({products:p.data.length,categories:c.data.length,best:p.data.filter(x=>x.isBestSeller).length}))},[]);return <><h1 className="font-display text-5xl font-semibold">Overview</h1><p className="mt-2 text-sm text-espresso/50">Manage the public cafe experience from one place.</p><div className="mt-8 grid gap-4 sm:grid-cols-3">{[['Products',d.products],['Categories',d.categories],['Best Sellers',d.best]].map(([l,n])=><div key={l} className="rounded-3xl bg-white p-6 ring-1 ring-black/5"><p className="text-sm text-espresso/50">{l}</p><p className="mt-2 font-display text-5xl">{n}</p></div>)}</div></>}

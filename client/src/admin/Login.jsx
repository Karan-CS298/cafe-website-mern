import {useState} from 'react'; import {Navigate,useLocation,useNavigate} from 'react-router-dom'; import {useAuth} from '../context/AuthContext'; import {inputClass,SaveButton} from './ui'; export default function Login(){const {admin,login}=useAuth();const nav=useNavigate();const loc=useLocation();const [email,setEmail]=useState(''),[password,setPassword]=useState(''),[error,setError]=useState(''),[busy,setBusy]=useState(false);if(admin)return <Navigate to="/admin" replace/>;const submit=async e=>{e.preventDefault();setBusy(true);setError('');try{await login({email,password});nav(loc.state?.from?.pathname||'/admin')}catch(e){setError(e.response?.data?.message||'Unable to sign in')}finally{setBusy(false)}};return <div className="grid min-h-screen place-items-center bg-cream p-5"><form onSubmit={submit} className="w-full max-w-md rounded-[2rem] bg-white p-7 shadow-xl ring-1 ring-black/5"><p className="text-xs font-bold uppercase tracking-[.25em] text-gold">Private area</p><h1 className="mt-3 font-display text-5xl">Cafe CMS</h1><p className="mt-2 text-sm text-espresso/50">Sign in to manage the public website.</p><div className="mt-8 space-y-4"><input className={inputClass} type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input className={inputClass} type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required minLength={8}/>{error&&<p className="text-sm text-red-600">{error}</p>}<SaveButton disabled={busy}>{busy?'Signing in…':'Sign in'}</SaveButton></div></form></div>}

import {createContext,useContext,useEffect,useState} from 'react'; import {api,clearCsrf} from '../services/api'; const C=createContext(null); export function AuthProvider({children}){const [admin,setAdmin]=useState(null);const [loading,setLoading]=useState(true);useEffect(()=>{api.get('/admin/me').then(r=>setAdmin(r.data.admin)).catch(()=>{}).finally(()=>setLoading(false))},[]);const login=async(credentials)=>{const r=await api.post('/admin/login',credentials);setAdmin(r.data.admin);return r.data.admin};const logout=async()=>{await api.post('/admin/logout').catch(()=>{});clearCsrf();setAdmin(null)};return <C.Provider value={{admin,loading,login,logout}}>{children}</C.Provider>} export const useAuth=()=>useContext(C);

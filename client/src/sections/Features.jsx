import * as Icons from 'lucide-react';
import { useCafe } from '../context/CafeContext';
import SectionHeading from '../components/SectionHeading';
import Reveal from '../components/Reveal';

export default function Features(){const {data}=useCafe();return <section className="bg-[#fbfaf7] px-5 py-28 md:px-8 md:py-36"><div className="mx-auto max-w-7xl"><SectionHeading center eyebrow="The experience" title="More than a coffee stop." description="A space designed around good coffee, comfortable hours and the little details that make you want to return."/><div className="mt-14 grid overflow-hidden rounded-[2.5rem] border border-black/[.06] bg-white md:grid-cols-2 lg:grid-cols-3">{data.features.map((f,i)=>{const Icon=Icons[f.icon]||Icons.Coffee;return <Reveal key={f._id} delay={i*.06}><div className="group h-full border-black/[.06] p-8 transition duration-500 hover:bg-cream md:border-b md:border-r lg:p-10"><div className="flex items-center justify-between"><div className="grid h-12 w-12 place-items-center rounded-full bg-cream text-coffee transition duration-500 group-hover:scale-110 group-hover:bg-espresso group-hover:text-gold"><Icon size={21} strokeWidth={1.5}/></div><span className="font-display text-3xl text-espresso/15">0{i+1}</span></div><h3 className="mt-16 font-display text-3xl font-semibold tracking-[-.02em]">{f.title}</h3><p className="mt-3 text-sm leading-6 text-espresso/50">{f.description}</p></div></Reveal>})}</div>{!data.features.length&&<p className="mt-10 text-center text-sm text-espresso/45">Cafe features will appear here soon.</p>}</div></section>}

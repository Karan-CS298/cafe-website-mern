import {MessageCircle} from 'lucide-react'; import {whatsappUrl} from '../utils/whatsapp'; import {useCafe} from '../context/CafeContext'; export default function WhatsAppButton(){const {data}=useCafe();return <a aria-label="Order on WhatsApp" href={whatsappUrl(data.settings.whatsapp,`Hello ${data.settings.cafeName||'Cafe'}, I would like to place an order.`)} target="_blank" rel="noreferrer" className="fixed bottom-5 right-5 z-40 grid h-14 w-14 place-items-center rounded-full bg-[#25D366] text-white shadow-xl transition hover:scale-105 md:bottom-7 md:right-7"><MessageCircle size={25}/></a>}
